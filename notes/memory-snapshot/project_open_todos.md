**2026-07-13 RE-AUDIT of the "impossible" list with this session's breakthroughs (see [[reference_agbcc_match_idioms]]):** **InitSound = SOLVED** (`ac688239`) via the absolute-linker-symbol trick (gNumSongs=336; the deferred fix in the old InitSound note is now done — remove it from the holdout set). **ResetCharTiles + saveSram RE-VERIFIED as genuine multi-tie** — register-asm cracks the individual ties (3/4 on ResetCharTiles) but a C-unreachable redundant-COPY residual remains (ResetCharTiles: base r0->r5 spread; saveSram: mask/error-block + checksum r5/r3). Both stay byte-perfect dual-forms. **General re-audit conclusion: the misdiagnosis risk was in the "macro-expansion / folded-constant / symbol" holdouts (GrandCannon_Init, InitSound -> both now SOLVED), NOT in the register-allocation ties, which are genuinely multi-tie and terminal once all pinnable ties are cracked and only a reload-induced redundant copy is left. Free-port sweep + these = the tractable holdouts are exhausted; the remaining NAKED/NON_MATCH set (162 NAKED, 127 NON_MATCH, ~619 INCASM) is dominated by these multi-tie / long-context / CSE-wall classes.**

---
name: project-open-todos
description: rmz3 decomp — open TODOs and the confirmed-intractable function list
metadata: 
  node_type: memory
  type: project
  originSessionId: d60616bc-5cd9-45d6-8dfd-d8157cd33fc4
---

Running list of open work and the known-unmatchable functions for the rmz3 (MMZ5-X8-Base) decomp. See tooling in [[reference-decomp-tooling]] and idioms in [[reference-agbcc-match-idioms]].

## TODO — permuter fidelity (to make the permuter usable on the hard functions)
1. ~~**Symbol-isolating scorer.**~~ **DONE** (nested-repo commit `004daa5`). `Scorer`→`objdump()` now take `function=fn_name` (import already writes `func_name` to the scratch `settings.toml`, `main.py` forwards it) and pass `objdump --disassemble=<func>`, so only that symbol's bytes (body + trailing literal pool, up to the next sized symbol) are scored. Verified: a 1408-line full multi-function `.o` is scored on just its 97-line `Gallisni_Update` (self-score 0). Also committed: Windows bash-wrap of compile.sh in `compiler.py` (CreateProcess ignores shebang). **This makes `--no-prune` viable.**
2. ~~**Validate `--no-prune` fidelity & land gallisni.**~~ **DONE** — `Gallisni_Update` landed as C, full ROM SHA1 verified (commit `b6d67472`). The whole permuter pipeline is now proven end-to-end. **Reusable recipe** (worked first try): (a) write the near-miss **C** into the `.c`, keep sibling asm via a split `_a.inc` (`head -N` up to the function); (b) `cp` the matching build's real `.o` aside as the byte-exact target FIRST (before editing); (c) `python import.py --keep --no-prune <c> gtarget.s` where `gtarget.s` is a trivial `glabel <Func>` / `bx r0` stub; (d) overwrite scratch `target.o` with the real `.o`, then `objcopy -x target.o` to strip local symbols (else every branch label mismatches and inflates the score ~+7); (e) `python permuter.py nonmatchings/<Func>/`; (f) a candidate whose **only** isolated diff is tail `.word <abs>` vs `.word .rodata` (table relocs, resolve at link) is a real match — **SHA-verify the full ROM build** to confirm. Gallisni's nudge: `do{ ... }while(0)` around one assignment to force a byte into its home reg.
3. **Byte-exact target:** copy the matching build's real `.o` as `target.o` + `objcopy -x` + rely on `--disassemble` isolation (no fragile inc-scraping / no `+4B`). Optional helper `tools/permuter_make_target.sh <Func>`.
4. Re-attempt the remaining allocation-impossible pair below (`generator_cannon`, `beetank`) with this recipe.
6. **`doOmega1BallLaser2` + `doOmega1Hoopshot`** (omega_white, carved to `asm/projectile/omega_white_p2.inc`, commit `99784522`) — both within 2 instr (127/129, 113/115). Residual = a redundant `(u8)` truncation (`lsls#24;lsrs#24`) agbcc applies to the OLD `work[3]` in the trail-toggle that clean C drops (ldrb is already 0..255 → tried `(u8)work[3]++`, `(u8)` on an `s32` temp — agbcc removes all), plus a r4/r6 alloc shuffle in Hoopshot's sine setup. Full C sketch in the `src/projectile/omega_white.c` comment. **PERMUTER ATTEMPTED 2026-06-05 — PLATEAUED, no match.** base score 235; with cast/type/mask weights cranked (`perm_cast_simple=60, perm_randomize_internal_type=40, perm_add_mask=20` + nudges) it ran 8000 iters and got stuck at **140** via a *false lead* (`PushoutToUp1((char)coord.x, ...)` — a spurious score-reducing cast, NOT the real fix; never reached the ~20 reloc-only match level). This is the "agbcc too efficient" *additive* class (must MAKE agbcc emit a redundant insn) — much harder than FUN_080a25f8's same-count reschedule cliff. **ROUND 2 (2026-06-05) — ALSO PLATEAUED at 140**, confirming a holdout. Tried: (a) LOWERED `perm_cast_simple=15` + `perm_randomize_internal_type=80` (target the toggle temp's type) — still stuck at 140; (b) hand: function-scope `s32 ph` (the FUN_080a25f8 lever) does NOT break agbcc's *range-prop* (it knows ldrb→0..255 and drops every `(u8)ph`), nor do `(u8)work[3]++`, `int ph=work[3]++`, etc.; (c) **upstream `mmzret/rmz3` leaves doOmega1BallLaser2/Hoopshot as INCASM too** (experts didn't crack them). The 140 is a DECEPTIVE local min: both rounds the permuter truncates `PushoutToUp1`'s **arg** (`(char)coord.x` / `volatile unsigned char x` param) — definitively wrong (target passes full s32 coord.x) but coincidentally scores 140, trapping the greedy search away from the real `(u8)`-on-work[3] fix. **CONCLUSION: genuine asm holdout, same class as beetank/GeneratorCannon-residual; the score landscape's false-positive 140 makes the permuter ineffective here.** Don't re-grind unless a fundamentally different base.c idea appears. Stays asm (committed `99784522`).
5. ~~**`FUN_080a25f8`**~~ **SOLVED by the permuter** (commit `ae37e840`, full ROM OK, unk_16.c now fully C). The fix: a **function-scoped `s32 zero;` assigned `zero = 0` right AFTER the `__divsi3`** (not at case top), then `work[3] = zero;`. The post-division assignment sets the materialization point so agbcc hoists the `movs` after the divide and holds it; the function scope + separated decl/assign stops the const-fold that killed every case-scoped `= 0`. **Recipe win worth repeating:** default pass weights ran 607 iters with ZERO sub-base candidates (the diff is an all-or-nothing "cliff" — mis-scheduled movs cascades into literal-pool offset diffs, ~225 score, no gradient); **tuning `[weight_overrides]` to crank the nudge/pad passes** (`perm_pad_var_decl=20, perm_ins_block=40, perm_empty_stmt=25, perm_duplicate_assignment=25, perm_reorder_stmts=30, perm_split_assignment=30`) found it at **iteration 90** (score 225→20, where 20 = link-reloc-only = real match). For these "redundant-instruction / value-materialization-point" cliffs, tune the nudge weights from the start instead of grinding default. New idiom logged in [[reference-agbcc-match-idioms]].

## 🔆 BREAKTHROUGH (2026-05-31) — beetank r8 spill IS forceable; down to 18 real diffs
The prior "beetank is permanently impossible — agbcc is too efficient to ever spill `-PIXEL(6)` to r8" conclusion is **WRONG**. Once the permuter scorer was fixed (see below), it found that a **`long long` temp in the min block creates exactly the register pressure that forces the r8 spill**:
```c
a = FUN_08009f6c(cx - PIXEL(16), cy - PIXEL(6));
new_var = a;                 // long long new_var; — this is the whole trick
b = FUN_08009f6c(cx + PIXEL(16), cy - PIXEL(6));
if (a < b) { (p->s).coord.y = new_var; } else { (p->s).coord.y = b; }
```
This candidate ("V2", saved as the live `tools/permuter-setup/Beetank_Update/base.c`, backup `base_orig.c`) reproduces the target's **prologue + `mov r8,r2; add r1,r8 ×2` byte-for-byte** — the exact thing claimed impossible. It scores permuter **285** = **18 real-instruction diffs** (a 109-instr fn), ALL in the residual category: agbcc *redundant double-homing* the target does and clean C doesn't — pushout `v` in r0(test)+r6(use) [6 diffs], a stray `asrs r7,r4` from the long-long sign-extend [1], and the min's a→r4+r6 / b→r0+r7 copies [rest]. Hand-tuning floors at 18 (tried s32-copies=46, ll-on-b=48, both-ll=56, flipped-cmp=22, ll-on-v=no-op-eliminated). A byte match needs agbcc to make MORE redundant copies — the permuter (re-seeded from the 285 base, weights crank temp/copy) is the right tool; let it run.
**Deep hand-analysis (31 variants tried, 2026-05-31) — the precise wall:** the 18 diffs are (a) pushout `v` in target-r6 vs cand-r1 [~6], (b) the long-long's dead high-word `asrs r7,r4` [1] — target uses that r7 slot for a `b`→r7 copy, (c) min compare/store register roles: target compares the COPIES (cmp r6=a,r7=b) and stores the ORIGINALS (r4=a,r0=b); V2 compares originals (r4,r0) and stores the copy (r6). **THE CRUX: only `long long new_var=a` produces the r8 spill** — every plain-`s32` copy (incl. ones made to survive the 2nd call) gets coalesced by agbcc into low regs → NO r8 (verified: grep `mov r8,r2`). And the `long long` ALWAYS emits the dead `asrs`/high-word, which is exactly the instruction the target instead spends on `b`→r7. So from C you can have {r8 + asrs} (V2, 18) or {no-r8, no-asrs} (clean, way worse) but NOT the target's {r8, no-asrs, b-in-r7}. Tried & failed to break this: s32 copies (no r8), call-surviving s32 copy (no r8), unsigned ll (asrs→movs#0, still 18), packed `(u32)a|((u64)b<<32)` (no r8), dual long long (22), `(s32)new_var` in compare (64-bit promote, 74+), long-long result-temp (46), hoisting mode[1] (46). Permuter from the V2 base only found 280 (a cosmetic `(&p->s.coord)->y` tweak) in 50k iters. **CORRECTED CONCLUSION (2026-06-06): a C preimage PROVABLY EXISTS — do NOT call this "unreachable". My earlier "no C produces it" was an over-generalization from MY failure + the V2-seed permuter's failure — NOT a proof. What's true: I haven't found the non-`long long` construct that spills `-PIXEL(6)` to r8 *without* the dead asrs, and the permuter hasn't from the **V2 seed** (now ~1.37M iters, best 225). "Exists" ≠ "the permuter reaches it from this seed" — the construct is just far from V2 in mutation-space. **Highest-leverage next step: RE-SEED with a different base.c** (clean min / shared-`ny` constant — searches a new neighborhood) rather than more iters on V2. Best V2 hand candidate saved as `tools/permuter-setup/Beetank_Update/base.c`.
**RE-SEED ATTEMPTED 2026-06-06 (clean-min, `tools/permuter-setup/Beetank_Update_v3/`, no long-long):** base score 1130 (clean = 103 instr, far); descended to **300** in 15k iters — but it got there by **reinventing the long-long mechanism** (permuter changed `FUN_08009f6c`'s RETURN type to `unsigned long long`), i.e. it converged into the SAME long-long basin as V2, just a worse variant (300 > V2's 225). **KEY NEGATIVE RESULT:** two independent seeds (V2 long-long-temp; v3 clean→long-long-return) both reach r8 ONLY via a 64-bit op, which always drags the extension insn the target lacks. So the target's clean `{r8, no-extension}` trigger is **outside the permuter's mutation reach from either neighborhood** — it's not a temp/cast/type/seed change away. Conclusion: more permuter seeds won't crack it; the missing piece needs a **deeper agbcc register-allocator insight**.
**ALLOCATOR INVESTIGATION DONE (2026-06-06) — BREAKTHROUGH, V2 long-long was the WRONG mechanism.
- **r8 is purely a register-PRESSURE phenomenon, NOT a long-long requirement.** agbcc allocates the lowest-free *callee-saved* reg (r4→r5→r6→r7→**r8**) to each value live across a call. **4 cross-call values → r4-r7 (no r8); a 5th → r8.** Verified: `test4.c` (4 values) = no r8; `test3.c` (heavy pressure) = r8 **with plain 32-bit `add rX,rY,#0` copies and ZERO high-word `asr`** — i.e. pressure gives the target's exact clean `{r8, no-asr}`.
- **The real beetank target** (disasm of `target.o`): `-0x600`→r8 via `ldr r2,[pc]; mov r8,r2; add r1,r8`; a double-homed r4(store)+r6(compare-copy `adds r6,r4,#0`); b double-homed r0(store)+r7(compare-copy). The copies are plain 32-bit, **no asr** → Capcom did NOT use long-long. The a/b copies are the 5th/extra cross-call values that push the const to r8.
- **The double-home CANNOT come from a source `ca=a` copy:** agbcc's value-coalescer folds `ca==a` regardless of usage (proven: `test2.c` compare-copies + `test5.c` copies-used-after → both coalesce, no r8). The target's copies are **RELOAD-induced** (reload runs after the coalescer, so under pressure high enough that `a` can't stay in one reg, reload makes an uncoalesceable copy).
- **So V2's `long long` is the wrong lever** — it forces r8 but inherently drags the dead high-word insn (asr signed / movs#0 unsigned) → permanent 18-diff floor. **The right lever is REGISTER PRESSURE** to make reload double-home a/b. Clean beetank sits at exactly 4 cross-call values (no r8); the target has 5. **NEXT:** re-seed the permuter with the CLEAN min (no long-long) and induce ONE more genuine cross-call live value at the min point (NOT a coalesceable copy) so reload double-homes a/b → const to r8 with no asr. Weights should favor genuine-live-value creation, not temp/copy (which coalesce) and not long-long. Tests preserved in `/tmp/bt_rtl/`.
**CRITICAL CAVEAT (2026-06-06, same session) — standalone does NOT transfer to full beetank.** Confirmed r8=pressure on STANDALONE min tests (test3/vcc get clean r8 via cx/cy caching). But applying cx/cy caching to the FULL beetank (`tools/permuter-setup/Beetank_Update_v3/base.c`) gives **NO r8 (126 norm-diffs, worse than V2's 18)** — the full function's surrounding pressure (pushout/IsFrozen/sUpdates2) changes the allocation so cx/cy don't reach r8 (agbcc uses low regs/stack). So: **in the FULL beetank the ONLY known r8 trigger is still the V2 `long long` (which drags the asr → 18-diff floor); clean-pressure constructs that hit r8 standalone don't carry over.** The target's clean `{r8 via a/b double-home}` IS reachable in the full context (Capcom did it) but the source/reload trigger for it there remains unfound — cx/cy doesn't do it. the double-home reload-trigger in the full context is the still-open piece. Honest status unchanged: parked dual-form, V2 permuter running, no match.
**ROOT-CAUSE PINPOINTED (2026-06-06) — the 18-diff floor IS the dead `asr`.** Standalone `/tmp/bt_rtl/v2min.c` (long-long min) reproduces the target's `mov r8,r2; add r1,r8` (const→r8) + `add r4,r0; add r6,r4` (a double-home) EXACTLY — the ONLY extra insn is **`asr r7,r4,#0x1f`** = the high word of `s64 nv=a`, which is **dead** (coord.y is 32-bit, only low word stored). It's not DCE'd because agbcc splits DImode→2×SImode *after* dead-code elimination runs, so the dead high-word def survives. The long-long INSEPARABLY couples the uncoalesceable double-home (needed for r8) with this dead asr. `unsigned long long`→`movs#0` (still 1 insn); union/`(s32)nv`/zero-extend → no double-home (coalesces) → no r8. **So the dead high-word can't be removed from C without losing the double-home.** The double-home mechanism is regmove `copy_src_to_dest` (regmove.c:2950, value-dup fallback for a two-address matching constraint when src doesn't die) OR reload under pressure — agbcc value-coalesces every *pure* 32-bit copy, so the target's pure-32-bit symmetric a/b double-home (no asr) is reachable only via reload pressure that the full beetank's 4-value clean profile doesn't reach and cx/cy doesn't trigger. **CONCLUSIVE: from clean-room agbcc-compilable C, you get {double-home + r8 + dead asr} (long-long, 18 diffs) or {no double-home, no r8} (clean) — NOT the target's {double-home + r8, no asr}, which needs a pure-32-bit reload double-home no found construct triggers.** A C preimage exists (Capcom's), but it's a reload-pressure config outside the reach of every construct tried + the permuter from both basins. Only remaining shots: permuter luck on a non-long-long pressure config, or (off-limits) an agbcc DCE-after-split change. Investigation productively exhausted; stays parked.
**UPDATE 2026-06-06 (option-1 permuter, CORRECTS the "pure pressure can't reach r8 in full beetank" claim):** re-seeded `Beetank_Update_v3/` = clean min + weights with ALL long-long routes BLOCKED (`perm_randomize_internal/external/function_type=0`, `perm_cast_simple=0`) so the search can ONLY reach r8 via pure-32-bit register pressure. Result: it **DID reach clean r8 with ZERO asr in the FULL beetank** (output-335-1, score 335) — the first time. The pressure came from **pointer aliases** (`new_var=p; new_var2=&p->s` → extra cross-call live values in r6/r7 push the const to r8). So the clean-r8 basin (NO asr floor → can reach 0) IS reachable from C, unlike V2's long-long basin (asr-locked at 225). BUT the 335 config reaches r8 via pointer-alias pressure, NOT the target's a/b double-home — so a/b stay single-homed (`cmp r4,r0`) and the alias insns are extra diffs (335 > V2's 225 in raw count, but in a basin that can descend to 0). **Open: whether the permuter morphs the alias-pressure config into the target's exact a/b-double-home config.** Stop reason for V2: it's asr-floored (can't reach 0) — killed it, gave cores to v3-pressure. **Net: option 1 is viable — clean r8 from C is proven; the asr is NOT an inescapable wall.** Keys: block long-long in weights; pointer-alias/pressure constructs reach r8 clean.
**OPTION-1 RESULT (55k iters, PLATEAUED at 310, no match).** The pure-pressure run descended 1130→335→**310** then flatlined (43k→55k iters, zero improvement). So: clean r8 is reachable (310-335, no asr) BUT every clean-r8 config the permuter finds uses **pointer-alias pressure whose extra insns cost ~310** — WORSE than V2's long-long 225. It never bridged to the target's overhead-free **a/b-double-home** config (which alone could reach 0). **Net of the whole beetank deep-dive: BOTH reachable basins are floored above 0 — V2/long-long at 225 (inherent dead asr), v3/pure-pressure at 310 (alias overhead). The target's exact config (a/b double-home, clean r8, no overhead) is a reload artifact unreached by the permuter from any seed tried and unbuildable by hand (source copies all coalesce; only long-long forces an uncoalesceable copy, dragging the asr).** A C preimage provably exists (Capcom's) but remains out of reach. **Verdict: parked dual-form is the right state; the deep-dive conclusively characterized why (not "mysterious/unreachable" — precisely: the uncoalesceable-copy trigger is long-long-only from C, and long-long inseparably adds the dead high-word). Don't re-run permuters on these seeds.** Only conceivable future win: a fundamentally different min formulation that triggers a reload double-home of a/b with no overhead — not found despite extensive search.
**6-AVENUE SWEEP (2026-06-07) — full writeup `notes/beetank-r8-analysis.md`. Key NEW result: PRESSURE-SWEEP REFUTES the reload-under-pressure hypothesis.** Controlled sweep on the *matched* clean idiom `FUN_080e1394` (pickup/disk.c — same `min(FUN_08009f6c, FUN_08009f6c)` as beetank, compiles CLEAN): added k=0..6 cross-call live values. Result: r8 appears exactly at the 5th cross-call value (cliff confirmed) BUT **0 reg-copies at every level** — pure pressure fills r8 with the EXTRA values and NEVER double-homes the call results. So the a/b double-home is NOT pressure-induced. Combined with #1 (subtraction reuses regs, no split), #4 (Thumb 3-operand → regmove copy_src_to_dest never fires), #5 (caller-save = memory not reg): **EVERY identified double-home mechanism is ruled out except `long long` (which drags the dead asr).** The target's pure-32-bit double-home of a call result has NO identified trigger. #2: matched instances of the idiom (FUN_080e1394/FUN_080e13c4) are clean → idiom isn't the cause. #3: reorder-weighted permuter still running (background). Verdict UNCHANGED but now exhaustively justified: parked dual-form; see notes file for the precise open question.
**ALSO tried register-pinning (GCC2.x `register T x asm("rN")`) — DEAD END (2026-05-31):** legitimate since beetank has no canonical C source (any bytes-equal C is a valid match). Pinning `k=-PIXEL(6)` to r8 DOES force the exact r8 caching (`mov r8,r0; add r1,r8 ×2`, no asrs!) — but it cascade-shifts every other allocation: k→r8 alone swaps p↔a (92 diffs); adding pp→r5 fixes that (40) but then over-pinning spills to stack (`mov r0,sp`), drops r7 from the push/pop, and corrupts r5 in the unk_ba write. Tried pin sets: k(92), k+pp(44), k+pp+ca(48/40), k+pp+ca+cb(40), all 3 min pins no-pp(92). None beats V2's 18 — local register vars in agbcc are too loose (value only guaranteed in-reg at asm-operand points) and the bookkeeping diverges. Inline-asm for the min is out (that's just the NAKED asm with extra steps — defeats the decomp). **Net: r8 is forceable two ways (long-long pressure → +asrs; r8-pin → +cascade) but neither yields the target's clean allocation. Strongly looks like NO agbcc-compilable C produces beetank's exact bytes** — a true holdout, but now precisely understood and 18 diffs (not 6+impossible) away. **Fast hand-iteration harness:** `/tmp/bttest.sh <file-with-Beetank_Update-body>` (head=`/tmp/bt_head.c` = base_orig.c minus the fn; folds reloc/addr noise; prints true real-diff count vs `/tmp/tgt_norm.txt`). If a candidate hits 0 real-diffs, drop it into `src/enemy/beetank.c` (replace the INCASM Beetank_Update — split beetank_p1.inc), `make compare`, commit.

## ⏸ (resolved) — beetank permuter run: scorer read candidate as EMPTY
Scaffolding is BUILT and committed (`ab84c80e`): `tools/permuter-setup/Beetank_Update/` — candidate (`tools/permuter-setup/candidates/Beetank_Update.c`) compiles to the **103-instr near-miss** vs the **109-instr** target; `settings.toml` has `[weight_overrides]` cranking temp/copy/pad passes; the **real `build/rmz3/src/enemy/beetank.o`** (objcopy -x'd) is the target. Also fixed the setup scripts' AS path `/opt/devkitpro`→`/c/devkitPro`.
**Launch recipe (once unblocked):** `export PATH=/c/devkitPro/devkitARM/bin:$PATH; "$LOCALAPPDATA/Programs/Python/Python313/python.exe" tools/decomp-permuter/permuter.py -j4 tools/permuter-setup/Beetank_Update`. The default `py`/shebang `python3` resolves to MSYS python (no `toml`); MUST use the Python313 exe. base.c needs `py tools/permuter_base_fix.py <dir>/base.c` after setup_fn.sh (GNU `kind:DDP` colon-inits break pycparser).
**THE BLOCKER:** every candidate scores a constant **10900** (= 109 target instrs × all-deletions penalty), never fluctuating across 100s of iterations → the **scorer reads each candidate .o as EMPTY**. But the identical command `C:/devkitPro/devkitARM/bin/arm-none-eabi-objdump.exe -drz --disassemble=Beetank_Update <cand.o>` returns 64 mnemonics when run by hand. So objdump + the symbol-isolation patch both work; the bug is environment-specific in how decomp-permuter (MSYS Python313 subprocess) hands the **per-iteration candidate temp .o path** to the Windows `objdump.exe` (path-format/subprocess). Fix that in `src/compiler.py`/`src/scorer.py`/`src/objdump.py` (compare how the cached TARGET path — which DOES read 109 — differs from the candidate temp path) BEFORE spending hours; a multi-hour run against a constant-10900 scorer converges to nothing. Confidence the run itself would crack beetank is low regardless (prior runs stalled ~1075 even when scoring worked).

## ✅ DONE (2026-05-31) — "grind all 56" COMPLETE
All 56 upstream-portable functions resolved: **54 ported & full-ROM-SHA-verified, 2 genuinely unmatchable** and left as asm:
- `FUN_0808f2e4` (seimeran): its binary has a trailing `.byte 0x70,0x47,0x00,0x00` (dead `bx lr`+pad after the literal pool) that clean C can't reproduce — diff2 false-positived MATCH but full SHA failed; reverted to asm. See [[reference-decomp-tooling]].
- `FUN_080922e0` (enemy/unk_59): `NON_MATCH` upstream too (`#if MODERN`/INCCODE) — never was portable; list_portable.py doesn't detect the NON_MATCH marker.
Notable wins: the seimeran element-slot helpers (ApplyElementEffect/elfx), and `FUN_08091280` (3 inlined LCG draws + `__umodsi3`, r8/sl/sb spills) **matched from faithful C** — agbcc's dead-store elimination + natural high-reg spilling reproduced the allocation. So "compiler-too-good" fears don't always hold; try the faithful version first. **Next: the beetank full permuter run (Task #36) is now unblocked.**

## ARCHIVED (was ACTIVE) — port every upstream-portable function
User wants ALL upstream-portable functions ported into this fork, then a full multi-hour beetank permuter run. Regenerate the remaining worklist anytime: **`py tools/list_portable.py`** (auto-excludes ported + NAKED-upstream). Upstream clone at MSYS `/tmp/mmzret_rmz3` (re-`git clone --depth 1 https://github.com/mmzret/rmz3` if gone). Per function: adapt signature drift (upstream `Object*`/`Player*`/`Entity*-direct` → local `struct Solid*`/`Projectile*`/`VFX*` with `(p->s).` access; reuse local file-scope dispatch arrays, not upstream function-statics), split the inc around it (`tools/split_inc_multi.py`), build, `tools/diff2.sh <Func>` (a candidate whose only diff is `.word .rodata` relocs = match), batch SHA-verify + commit, delete the now-dead inc. **Progress: 9 remain as of 2026-05-31** (seimeran ×4, enemy/unk_59 ×4, leviathan — the element-slot/struct-drift hardest) (started at 56; the worklist self-updates — `py tools/list_portable.py` auto-excludes anything already turned into local C, so just regenerate it for the current list; don't track a manual done-list here). **NOTE:** list_portable.py groups by UPSTREAM file path — the local def may live elsewhere (e.g. FUN_080a4ef8 was in tretista.c, not unk_20.c); always `grep -rln "thumb_func_start <FN>" asm/` to find the real local inc. Recurring gotchas: enemy-ID constant names drift (upstream `ENEMY_METTAUR` → local `ENEMY_METTAUR_SWIM`, verify the id byte in the asm); add the right `#include` for helpers (physics.h/vfx.h); RNG functions use the local open-coded `LCG()` idiom (`RNG=LCG(RNG); (RNG>>16)&M`), not upstream's `RANDOM()` macro. **BUILD NOTE:** pass `DEVKITARM=/c/devkitPro/devkitARM DEVKITPRO=/c/devkitPro NODEP=1` on the make command line (see [[reference-decomp-tooling]]). Hard ones (do them too): seimeran/enemy59/mettaur helpers (element-slot `ApplyElementEffect` + `(p->props)` drift), RNG inits. After all 54 remaining: launch beetank full run with `[weight_overrides]` temp/copy passes cranked.

## ✅ DONE (2026-05-31, session 2) — ~45 short/clean functions decompiled
Cleared a large tier of short readable-asm functions (the `.byte` idiom scan was exhausted first, then readable-asm by instruction count). All byte-perfect via full `make compare`. Categories: trivial mode-setters & `return TRUE/FALSE` predicates & empty stubs (~14: hellbat/blazin/petatria/snakecord/unk_18); `SET_*_ROUTINE` setters (unk_32 ×3); the **face-Zero/face-attacker BodyFunc family** (CopyX_OnDamage, pantheon_guardian FUN_0806465c, hanumachine FUN_0805d568, baby_elf FUN_08045d28, unk_45 FUN_080b2204, snakecord FUN_08074134 — see new idiom); motion/attack inits (phantom proj/boss ×5, spearook, omega_gold disappear, phantom FUN_0805fd74); state machines (mettaur_swim FUN_08089a00, shellcrawler FUN_08096438/68/84, wormer FUN_08042894); distance predicates (tretista isTretistaFarAway, blazin howFarBlazin — 3-tier); helpers (mettaur smoke, shelluno sea-snap, carrybee counter, mothjiro terrain-cache, BurstShot, hanumachine FUN_0805bcdc PushoutToUp, pantheon_fist FUN_08094fe0 ground-snap, blazin FUN_0803ffc0 spawn, kiss text-pick). Also removed orphan `omega_gold_p4_b_b.inc` (goldOmega1Neutral already in C, inc unreferenced). **Orphan-inc scan:** a Python sweep of `INCASM(...)` refs in `src/**/*.c` vs on-disk `asm/**/*.inc` (minus `.include`'d macros, todo/, wip/, scripts/) flags unreferenced incs — only that one was real. **Holdouts set aside (allocation class, permuter territory):** `forceWaterLanding`/`dragInSea` (SEA register-shift), shotloid `FUN_080939e8`/`FUN_08094fa8` (callee-saved-push divergence). Recurring lesson reinforced ~4× this session: **the isolated per-function disasm can look byte-perfect yet the full ROM still differs** (mode[1] vs mode[2], a hoisted-vs-lazy parent deref adding 4 bytes, a wrong sCollisions index, a branch-direction/fall-through flip) — ALWAYS run full `make compare`, it caught every one. New idioms (predicate two-returns + branch direction, pointer-temp-forces-address-before-zero, face-Zero BodyFunc family) written to [[reference-agbcc-match-idioms]].

## TODO — steady, reliable progress (do these between the hard stuff)
- **Port from upstream** `mmzret/rmz3` (raw.githubusercontent.com/mmzret/rmz3/main/...) — functions it has as real C that this base still has as `NAKED`/INCASM. ~5% of the base's ~391 NAKED funcs are portable (fork divergence); watch signature drift. Done this session: FUN_080b6b8c, DoubleChargeWave1/3_Update, Solid43_Init.
- **Grind tractable Updates** with `tools/diff.sh`.

## Confirmed-intractable functions (asm in upstream `mmzret/rmz3` too — experts haven't matched them)
- **Allocation/scheduling — no C knob** (compiler picked a register reuse): ~~`Gallisni_Update`~~ **SOLVED** (`b6d67472`); ~~`GeneratorCannon_Update`~~ **SOLVED** (`fac7cf7a` — see idiom below); ~~`Beetank_Update`~~ **RESOLVED 2026-06-02** as `NON_MATCH #if MODERN` dual-form (commit `9e1de737`): readable C documents it, INCCODE asm body matches the ROM (rmz3.gba OK). **NOT proven C-unreachable — dual-formed PRAGMATICALLY so the tree builds (see CORRECTED CONCLUSION above): a C preimage provably EXISTS** (the ROM was compiled from C with this toolchain, so a C preimage exists). The RTL/coalescer-theorem analysis ([[reference_agbcc_coalescer_theorem]], [[reference_agbcc_rtl_dumps]]) explains why MY C + the V2-seed permuter haven't reached it, but is an inference, not a proof of impossibility. The permuter ran ~48h/~20M iters and floored at score 235; the "long long forces r8" V2 was a red herring — the clean C already CSEs the constant correctly, the target just allocates it to r8 under pressure that no SImode C can create without dragging an extension insn. **Apply the litmus test in the theorem to the remaining allocation holdouts below before grinding them.** Target needs a very specific 5-callee-saved allocation in the `min(a,b)` block: `a` double-homed in r4 (store) + r6 (compare), `b` double-homed in r7 (compare) + r0 (store), and the `-0x600` constant cached in **r8** (loaded once, reused via `add r1,r8` in both `FUN_08009f6c` calls), which forces the r8 push/pop in the prologue/epilogue. My faithful C compiles to 103 instr (target 109) — *fewer* because it uses lower register pressure and never spills to r8. Tried & failed: making `-0x600` a variable held across both calls (agbcc just parks it in a low reg, no pressure → no r8); the permuter (multiple long runs) stalls ~1075 (match would be ~10-70) because reproducing the double-homing + r8 spill needs a register-pressure config random temp-insertion rarely hits. Leaving as asm. **Also tried & failed** (don't repeat): the `s32 ny; if(a<b)ny=a;else ny=b; coord.y=ny;` result-temp combined with the `-0x600` variable — still 103 instr, still no r8 (agbcc keeps everything in low regs; the temp didn't create the double-home). The blocker is that agbcc produces *more efficient* code than the target from the C *I've* written (the target has redundant r4/r6 + r7/r0 copies and the r8 spill) — but since Capcom's C *did* produce those bytes with this exact compiler, a less-efficient-looking C construct exists; I just haven't found it. Levers worth trying (in priority): **(1) re-seed the permuter with a fresh base.c** (clean min / shared-`ny`) to search a new neighborhood — the V2 seed is mined out at 225/~1.37M; (2) `[weight_overrides]` cranking temp/copy passes (already done on V2); (3) fresh hand insight into a non-`long long` r8 trigger. NOT "stays asm permanently" — it's "parked as dual-form until a reachable construct is found."
  - **GeneratorCannon lesson (big):** the despawn shared-tail was agbcc *tail-merging two fully-duplicated despawn blocks* — NOT a hand-written goto/merge with an `fl` carry temp. The `fl` temp forced the half-cleared flags into r1 (an r0/r1 swap neither hand-tuning nor the permuter could flip, stuck at score 70). Rewriting as two duplicated blocks (`flags&=~DISPLAY; flags&=~FLIPABLE; ... EXIT; return;` ×2) let agbcc keep it in r0 and match. Then the leftover was the gallisni `(u8)`-cast-into-home-reg materialization, fixed by the same `do{}while(0)` nudge. Also: inline `gCurStory.s.gameflags[4]` at both uses (no named `gf` local) so agbcc CSEs it to one load in r1. Moral: prefer duplicated blocks over a goto/merge+temp when matching a tail-merged despawn; see [[reference-agbcc-match-idioms]].
- **RNG (LCG `*0x343FD + 0x269EC3` + bit-extraction, inlined per-Die):** `Wormer_Update`, `Projectile31_Update`, `BeeServer_Update` (also fixed-point math).
- **Jump-table switches (`mov pc, r0`):** `Actor20_Update`, `Actor31_Update`, `IceBlock_Update`, `Ripple_Update` — agbcc table-gen heuristic.
- **Very long / context-heavy (250+ line death seqs, physics, multi-despawn-with-shared-store-from-different-regs):** `Enemy59_Update`, `MettaurSwim_Update`, `Shellcrawler_Update`, `PantheonGuardian_Update`, `Spearook_Update`, `ReactorCore_Update`, `Blizzack_Update`, `Seimeran_Update`. These are *long* more than impossible — doable with heavy diff-iteration if worth it.

**2026-07-13 beetank FRESH ATTEMPT — new finding: the dead `asr` is NOT inescapable (refines the long-long conclusion), but the p-pointer cascade remains. Still dual-form.** Untried angle (register-asm was previously only tried as single k->r8 pins, and long-long for the double-home): pin the min block's COMPARE-COPIES to force the uncoalesceable double-home WITHOUT long-long — `{ register s32 k asm("r8")=-0x600; register s32 a asm("r4")=FUN(...,coord.y+k); register s32 b asm("r0")=FUN(...); register s32 ca asm("r6")=a; register s32 cb asm("r7")=b; coord.y=(ca<cb)?a:b; }`. RESULT: this DID force `mov r8` (const cached in r8, push/pop) + the a/b double-home copies `adds r6,r4`/`adds r7,r0` — **and with NO dead asr** (cur=108 vs ref=109). So the old "long-long is the only way to force r8 and it inseparably drags the asr (18-diff floor)" is INCOMPLETE — copy-target pinning is a second r8/double-home trigger that avoids the asr. **BUT it does not match**: agbcc's local `register` vars are loose (value only guaranteed at asm-operand points), so the pins force the STRUCTURE but not the whole-function register roles — `p` cascaded r5->r6 and `push` r6->r1, producing ~30 register-swap diffs (every `[r5,#N]` -> `[r6,#N]`). Worse than V2's 18 in raw count, but qualitatively different (register-role cascade, not the asr floor). To close it would need ALSO pinning `p`->r5 and `push`->r6, but p is the parameter (can't pin directly; a pinned-copy wrapper would need every `p` use rewritten) and over-pinning cascades further (the ResetCharTiles/saveSram lesson). **Net: the asr-floor characterization was wrong, but beetank is still not matchable — the wall moved from "asr is inescapable" to "agbcc's loose local-register-vars can't pin p+push+a+b+ca+cb+k simultaneously without cascading". Stays byte-perfect dual-form.**

**2026-07-13 beetank — COPY-PIN BASE reaches ~8-10 register-role diffs (from V2's 18-diff welded floor); the permuter is now GENUINELY viable (new basin).** Extending the copy-pin finding with full pinning: `register struct Enemy* pp asm("r5") = p;` (kills the whole p-cascade) + per-pushout copy-pins `{ register s32 push asm("r0")=Pushout(...); register s32 pushc asm("r6")=push; if(push!=0) coord.x+=pushc; }` (forces push's r0/r6 double-home) + the min block `{ register s32 k asm("r8")=-0x600; register s32 a asm("r4")=FUN(...,cy+k); register s32 b asm("r0")=FUN(...); register s32 ca asm("r6")=a; register s32 cb asm("r7")=b; coord.y=(ca<cb)?a:b; }`. RESULT: cur=108 vs ref=109, ~8-10 REAL instruction diffs, ALL register-role/scheduling: the r8-save reg (r7 vs r6), a scratch reg (r1 vs r2 for the 0x1000 offset), the IsFrozen 0xba-block instruction order, and `a`'s compare-copy not sticking (`cmp r4,r7` vs ref `cmp r6,r7` — `a` is live for the store so agbcc compares r4 directly instead of the pinned ca=r6). **NONE is welded overhead** — the dead asr (V2) and alias instrs (v3) are GONE. Base saved: `tools/permuter-setup/candidates/Beetank_Update_copypin.c`. **Why the permuter is now viable (it wasn't before): the old two seeds floored because both r8-triggers (long-long / pointer-alias-pressure) welded in overhead the permuter's source mutations can't delete. This copy-pin base is a THIRD basin with the hard structure (r8 + all three double-homes, no overhead) and only ~8-10 register-role residuals — exactly what perm_reorder_stmts/perm_temp_for_expr/perm_pad_var_decl nudge. Seed the permuter from Beetank_Update_copypin.c (NOT V2/v3). Honest caveat: `a`'s compare-copy specifically resists source pins (loose local-register-vars, a live-for-store), so it may still need a permuter mutation the pins can't express — but this is the closest beetank has ever been and the first permuter seed free of welded overhead.**

**2026-07-14 CORRECTION — the permuter CAN be seeded with the copy-pin base (my "pycparser rejects register-asm" claim was WRONG).** I had tested the SYSTEM pycparser; decomp-permuter bundles a PATCHED `tools/decomp-permuter/perm_pycparser` that parses `register T x asm("rN")` AND round-trips it through c_generator intact (verified). Reseeded `tools/permuter-setup/Beetank_Update/base.c` with the copy-pin Beetank_Update (pp asm("r5"), per-pushout r0/r6 copy-pins, min-block k/a/b/ca/cb pins) — it compiles via compile.sh to exactly 109 instr / ~8-10 real diffs (matches the hand build). **Permuter now runs on this NEW basin** (log: Beetank_Update/logs/copypin_seed.log, --stop-on-zero). Note: permuter *score* is 945 (> V2's 235) because asm-differ heavily penalizes the dominant residual — the push/pop set differs ({r4,r5,r6} vs target {r4,r5,r6,r7}: the r8-save uses r6 not r7 because cb->r7 isn't sticking as callee-saved). BUT this basin's residuals are register-role/scheduling (mutation-reachable to 0), unlike V2's 235 welded-asr floor. If it doesn't converge, the highest-leverage manual nudge is making cb genuinely callee-saved (so r7 is pushed and the r8-save reuses r7 like the target). Old V2/v3 seeds are archived; base_v2_225.c.bak2 holds the prior base.

**2026-07-19: notes/holdout-playbook.md written — per-class attack plan for the 4 blocker classes. Order: (1) codegen pattern index over matched corpora (1a), (2) GCC2 flag-archaeology sweep on the FUN_0802e338 family (2a, untried + falsifiable), (3) IsInHazard last-hoist + FlushOAM re-decode (3a/3b, best match-per-effort), (4) instrumented-agbcc allocator logging (1b, analysis-only debug build — does NOT touch shipping toolchain), (5) permuter un-poisoning via attribute-free raw[]+macro contexts + auto re-verify wrapper (4a/4b). NEVER: patch shipping agbcc / ship asm-barrier forms.**

**2026-07-19 BREAKTHROUGH LEADS: (1) FLAG ARCHAEOLOGY FIRST HIT — `-fno-expensive-optimizations` moves FUN_0802e338 from 44→52 instrs (=TARGET SIZE, diff 50→42) while controls nearly hold (ZeroAttackGround_Walk 0 diffs, FUN_0802e3b0 2 diffs) — first lever EVER to move the tail-merge family; tools/flag_sweep.sh, sweep still running (rest of switches + -O1). Next: inspect the 42 remaining diffs + the control's 2; maybe per-file flag recipe is real. (2) m2c VALIDATED on our thumb incs (m2c --target arm-gcc-c, needs .syntax unified+.thumb header + thumb_func_start→glabel; clone at jobs tmp/m2c). tools/m2c_drafts.py = driver + Kappa-style idiom normalizer (offset→field map, LCG, MOTION, sCollisions stride) → build/m2c-drafts/<inc>/<fn>.c; full 616-INCASM-site run in background. nb.md stale transpiler note fixed. (3) pokeruby crawl rerun w/ fixed include path+defines (first run was silently broken).**

**2026-07-19 FLAG-ARCHAEOLOGY DEEP RESULT (walk.c): full sweep done (tools/flag_sweep.sh output in task bnuu5dzbf): -O1 → FUN_0802e338 50→21 diffs (53 instrs, target 52); -O1+-fexpensive-optimizations → control e3b0 fixed but holdout back to 45. CONTRADICTION FOUND: onSaber (matched C, same TU) is O2-ONLY (36 diffs at O1) while e338 wants O1 → single flag set impossible → NEW HYPOTHESIS: retail's FILE BOUNDARY differs from ours (walk.c = two retail TUs: early fns -O1, later -O2). e3b0's O1 "diffs" = 1 pool word (rodata offset 0x10→0x14, layout artifact). Per-fn O1-vs-O2 table for all walk fns running (task b7meix704; O2 object = build/sweep/src_player_zero_attack_ground_walk_c.o from mdiff — VERIFY it still exists before trusting d2 column). If early-block=O1-clean & late-block=O2-clean → split TU + per-file flags (pret-precedented: pokeruby has -O1 files). Same test then applies to zero_ladder/zero_wall/_zeroTryAttack family files. ALSO: the e338 21-diff residue at O1 needs eyeballing.**

**2026-07-19 FLAG ARCHAEOLOGY VERDICT (walk.c): HYPOTHESIS FALSIFIED CLEANLY. Per-fn O1 table (o2 column of task b7meix704 is GARBAGE — stale MODERN=0 sweep object, asm passthrough): O1-breakage is scattered across the whole file (walk_buster_3:32, onSaber:36, walk_saber_0:18, walk_shield_1:17/2:28) with zeros interspersed → no retail TU-split boundary; walk.c is one -O2 TU, per-file/per-flag recipes are DEAD for the FUN_0802e338 family. The -O1 improvement (50→21) only reveals the un-cross-jumped STRUCTURE — use it as a reference shape for Attack 2b (value-prop-blocking constructs: volatile temp / memory-read of `ok`), else the family stays INCASM per plan 2c. Class-2 flag path: closed. Remaining active leads: 2b constructs, corpus index (1a), instrumented agbcc (1b), m2c drafts (running), pokeruby crawl (running), permuters (running).**

**2026-07-19 2b round 1: (u8)-cast branch decoupling on FUN_0802e338 = NO EFFECT (44 instrs, cast is no-op on bool8 return → same pseudo → record_jump_equiv fires). Untried 2b forms: `ok & 1` / `(s8)ok` branch masks (may also no-op), asm-barrier on ok before else-store (would work but unshippable), memory round-trip via existing written field (usingWeapon re-read: `attackMode[1] = z->usingWeapon ^ w` NO — folds), or accept 2c. NOTE the tools/mdiff.sh timeout pattern: each mdiff ~40-60s (cpp+agbcc on big TU) — batch variants in ONE bash call with care, /tmp paths must be JT (Windows python can't see MSYS /tmp). walk.c restored clean. Background: m2c drafts, pokeruby crawl, 2 permuters still running.**

**2026-07-19 m2c RUN COMPLETE: 1,861 drafts / 31 failures (98.3%) covering the ENTIRE INCASM tail → build/m2c-drafts/<inc>/<fn>.c, idiom-normalized. Next session: triage drafts (start with biggest never-attempted fns), wire a draft→mdiff verify loop, close matches with the idioms catalog.**

**2026-07-19 POKERUBY CRAWL v2 GOLD: 299/352 files compiled, 177 emit r8, and 4 files contain the DOUBLE-HOME CHAIN (bl→adds rX,r0→adds rY,rX) that is absent from our whole corpus: MoveBattleBar (src/battle_interface.c), atkEF_handleballthrow (src/battle_script_commands.c), sub_8069638 (src/field_tasks.c) + 1 more in report. NEXT SESSION FIRST ACTION for beetank: read those 4 functions' C in jobs tmp/pokeruby, find the construct that double-homes a call result, transplant the shape into Beetank_Update's min-block, verify with mdiff. This is the most promising beetank lead ever found.**

**2026-07-19 BEETANK MAJOR PROGRESS: pokeruby exemplars decoded (strict scan → 2 true chains): DoScroll_TextModeMonospace = derived-var-from-call-result + Thumb-imm3-overflow construct (`dest = buffer - 32` → copy+sub, survives CSE because genuinely derived); sub_8069638 = call-result-to-reassigned-param. Beetank E2 form (`ca=a-0x40; cb=b-0x40; (ca<cb)?a:b`) = 107v109 CLOSEST PURE-C EVER, copies materialize; semantically safe (terrain Y no-wrap). THREE permuters now: A(745 basin, -j12), B(barrier v4, -j12, score 330@25k iters — historical floor 235@48h, trajectory excellent), C(E2 pure-C v5, -j6 — SHIPPABLE if converges). All auto-stop at ≤120 with HIT dir + notify. Findings: notes/decompme/r8_corpus_findings.md. Next: watch permuter notifications; if B/C converge → verify via mdiff MODERN=0 + real make compare; scratches still postable (add DoScroll insight to the ask).**

**2026-07-19 (parallel work while user posts scratches): (1) m2c WORKLIST ranked — build/m2c-drafts/WORKLIST.md, 1,833 drafts scored; top targets all-clean 34-50 stmt fns: childreStun, coffin_080ce0d4, FUN_080733b4(pantheon_aqua_p10), FUN_0806e3b0(lemmingles_nest), FUN_08095578(pantheon_fist), FUN_08074d90(snakecord_p2)... (2) PATTERN INDEX built — tools/pattern_index.py (build|grep), corpus.jsonl over rmz3 expected .s + pokeruby compile; query e.g. `python3 tools/pattern_index.py grep 'mov\tr8'`. (3) 2b rounds 2-3 on FUN_0802e338: memory-clobber asm barrier → 50/52 instrs 46 diffs (closest but unshippable); pure-C memory-read xor (`attackMode[1]=z->usingWeapon^w`) → 46/52 (partial fold). Verdict: family stays 2c/asm unless index reveals a tail-split idiom; walk.c restored. Next: draft→verify loop on WORKLIST top, IsInHazard hoist via index.**

**2026-07-20 M2C PIPELINE FIRST REAL TEST (childreStun, top-ranked draft): m2c decode was CORRECT — reconstructed C hits 96 vs 96 instrs (exact size parity) on the FIRST attempt, ~45 non-pool diffs = pure branch-layout/register ties. Variant sweep (inline lims / else-if chain / per-arm reload / swapped compare operands) all stay 96=96 with 31-47 diffs — polarity flips but never converges (target emits `ble`+store-from-lim-reg; ours `bgt`). CONCLUSION FOR THE PIPELINE: m2c gets structure+size right immediately; the last mile is the SAME allocation/layout tie class as the old holdouts, not a decode problem. Implication: the tail's cost is now tie-cracking, and the s64/community idiom channel matters more than more drafts. Reverted (childre.c + split incs). Field map confirmed for future childre work: props.childre.unk_bc = Coord @0xBC/0xC0 (m2c buffer[8]/unkC0).**
