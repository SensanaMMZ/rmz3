- [Decomp build verification](feedback_decomp_build_verify.md) — always force-rebuild the .o file before trusting a SHA1 match; stale objects can lie
- [agbcc match idioms](reference_agbcc_match_idioms.md) — ★`register T x asm("rN")` CRACKS register-alloc ties (ladder, permuter-plateaued); chained-assignment `a=b=c=x`; jump-table/switch state-machine dispatch matches cleanly; bitfield shift-extract; `gGameState.z2` names the player ptr; ⚠NEVER `rm -rf build` + a `.c` rename needs `linker/*.txt` edits
- [Decomp tooling](reference_decomp_tooling.md) — use tools/diff.sh (objdiff % match) + refresh-expected.sh; permuter_compile.sh harness; **upstream mmzret/rmz3 is a git remote (`git fetch upstream`, branch upstream/dev) — `notes/upstream-free-ports.md` lists 241 functions it matched that we ship as asm; upstream also uses NON_MATCH (~106)**
- [Open TODOs & intractable list](project_open_todos.md) — permuter-fidelity fix path + the confirmed-unmatchable functions (gallisni/generator_cannon/beetank + RNG/jump-table/long categories)
- [Beetank r8 analysis](../../../../MMZ5-X8-Base/rmz3/notes/beetank-r8-analysis.md) — repo writeup (notes/beetank-r8-analysis.md): r8=register-pressure not long-long; double-home is a reload reg-copy artifact (NOT regmove/caller-save/source-copy); long-long floors at 18 (dead asr), pure-pressure at 310 (alias overhead); precise open question for a GCC2 allocator expert.
- [MSYS↔Windows paths](reference_msys_windows_paths.md) — the `cygpath -m` rule: normalize every path before it reaches a tool when mixing MSYS + Windows binaries (the /tmp-path bug that froze the permuter)
- [Split-inc verify](feedback_split_inc_verify.md) — never `rm` a split_inc_multi `_pN` part without grepping it first; only `_p1` is empty and only when the target func is first in the file
- [Permuter logging](feedback_permuter_logging.md) — always tee permuter stdout to a logfile so the cumulative iteration counter is captured (not just new-best scores)
- [agbcc RTL dumps](reference_agbcc_rtl_dumps.md) — agbcc supports GCC2 `-da`: emits .rtl/.cse/.cse2/.combine/.lreg/.greg/.mach pass dumps next to -o; use to trace why a pass picks a reg / injects an insn
- [SImode-coalescer theorem](reference_agbcc_coalescer_theorem.md) — "redundant register spreading" holdouts; **SUPERSEDED (2026-07-12): single register ties are NOT C-unreachable — pin the value with `register T x asm("rN")` (cracked ladder). Multi-tie funcs need one pin per tie. Try this before dual-forming any register tie.** See [[reference_agbcc_match_idioms]]
md) — agbcc is BYTE-IDENTICAL to the real 2. holdouts are source-reconstruction, not compiler bugs
- [Permuter packed-struct caveat](reference_permuter_packed_caveat.md) — permuter sandbox strips packing → FALSE matches for gOverworld/packed-struct readers; always confirm in real make build
- [RESUME STATE](project_resume_state.md) — 2026-07-12: upstream porting wired + ~10 dual-forms/NAKEDs retired (ports + register-asm trick); Blink→PaletteAnimation subsystem rename done; next = Camera/Renderer renames to unblock §A + per-tie register-asm on the backlog
- [NON_MATCH backlog](reference_nonmatch_backlog.md) — ranked closeness of all holdouts (modern_sweep.sh); which are 1-shot permuter targets vs need reconstruction
